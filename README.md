# update-api

A small server that Codium calls to check for the latest release. Forked from VSCodium's update-api, adapted for Colbe's Codium builds.

**Note:** The `updateUrl` field in `product.json` is set to `https://update.colbe.cc`, so neither Microsoft's nor VSCodium's update services are called.